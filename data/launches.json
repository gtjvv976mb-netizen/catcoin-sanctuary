{
  "note": "Launch signatures the hourly check proves first, whatever else names the wallet. Add a row { \"tx\": \"<launch signature>\" } after launching a cat (optional: the hourly scan also finds it). Each one is proved exactly like a scanned launch.",
  "launches": []
}

import fs from "node:fs";
const S = "/tmp/claude-0/-home-user-Cat-Intelligence-Agency/8cd510ac-033d-555a-89da-fcd5d6945b07/scratchpad/live";
const l = JSON.parse(fs.readFileSync(`${S}/lmeow.json`, "utf8"));
const p = JSON.parse(fs.readFileSync(`${S}/prefund.json`, "utf8"));
const t = l.token;
const lmeow = {
  note: "Live answers for 10 USDC -> LMEOW and that LMEOW back to USDC, and Jupiter's build of the buy for a funded mainnet wallet, simulated on the public RPC. LMEOW is a pump.fun cat coin (Token-2022, mint and freeze authority disabled per Jupiter's token API) whose only USDC pool is PumpSwap's ('Pump.fun Amm'); it is recorded for how its route is asked and what its swap charges the wallet, not as a coin to trade (Jupiter's token API flagged it isSus that minute). THE ASKS, api.jup.ag/swap/v1/quote, 100 bps, the exact queries under `query`: asked DIRECT (onlyDirectRoutes=true, restrictIntermediateTokens=true, no maxAccounts — the xStock venue's ask and the agent's first), Jupiter answered the PumpSwap pool both ways; asked for ONE HOP (onlyDirectRoutes=false, restrictIntermediateTokens=true, maxAccounts=24 — the agent's second ask), it answered 400 NO_ROUTES_FOUND both ways, the same minute. The buy's priceImpactPct was 0.0169 (1.69%, under the agent's 2% cap). THE BUILD (swapBuy): the body the agent sends for a direct route (no useSharedAccounts, no SOL wrapping, dynamicComputeUnitLimit false, a 50,000-lamport priority cap) for `user`, a system-owned mainnet wallet that held USDC and SOL and had never used PumpSwap, picked from the payers of recent USDC transactions and used here only as the simulation's fee payer — nothing was signed or sent. A route_v2 on the one PumpSwap pool, one idempotent create (the wallet's LMEOW account, under Token-2022). THE SIMULATION (`simulation`): simulateTransaction, sigVerify false, replaceRecentBlockhash false, commitment processed, on api.mainnet-beta.solana.com, with `addresses` read by getMultipleAccounts (processed) immediately before; both answered from slot preSlot = simSlot. The addresses are the agent's order: [wallet, its LMEOW account, its USDC account, its wrapped-SOL account, then every other account the transaction writes]. The wallet's lamports fell by 2,915,040: the 5,000 signature fee, the 50,000 priority fee, 1,513,840 for its new LMEOW account (170 bytes) and 1,346,200 for BJdXRvebJb5VbaPFyyDtZ9MLA2hBKasCpmcS8hXcQ3fX, a 137-byte account owned by the PumpSwap program (pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA) at the PDA of \"user_volume_accumulator\" and the wallet, which the swap opens with the wallet as payer. Both are exactly their rent-exempt minimums at the rate in `rentSysvar` (5,080 lamports a byte-year, threshold 1). No other account's lamports moved; the wallet's wrapped-SOL account (it held some) is untouched.",
  capturedAt: l.capturedAt,
  sources: ["https://api.jup.ag/swap/v1/quote", "https://api.jup.ag/swap/v1/swap", "https://lite-api.jup.ag/tokens/v2/search", "https://api.mainnet-beta.solana.com (getMultipleAccounts, simulateTransaction, getAccountInfo)"],
  user: l.user, settlementMint: l.settlementMint, tokenMint: l.tokenMint, symbol: l.symbol,
  token: { id: t.id, name: t.name, symbol: t.symbol, decimals: t.decimals, tokenProgram: t.tokenProgram, launchpad: t.launchpad, graduatedPool: t.graduatedPool, graduatedAt: t.graduatedAt, audit: t.audit, tags: t.tags },
  query: l.query,
  answers: Object.fromEntries(Object.entries(l.answers).map(([k, v]) => [k, { status: v.status, body: v.body }])),
  swapBody: l.swapBody,
  swapBuy: { swapTransaction: l.swapBuy.swapTransaction, lastValidBlockHeight: l.swapBuy.lastValidBlockHeight, prioritizationFeeLamports: l.swapBuy.prioritizationFeeLamports, computeUnitLimit: l.swapBuy.computeUnitLimit },
  lookupTables: l.lookupTables,
  simulation: { addresses: l.simulation.addresses, preSlot: l.simulation.preSlot, simSlot: l.simulation.simSlot, err: l.simulation.err, fee: l.simulation.fee, unitsConsumed: l.simulation.unitsConsumed,
    pre: l.simulation.pre, post: l.simulation.post, logs: l.simulation.logs },
  rentSysvar: l.rentSysvar,
};
fs.writeFileSync("/home/user/Cat-Intelligence-Agency/fixtures/agent/jupiter-usdc-lmeow-pumpswap.json", JSON.stringify(lmeow, null, 1) + "\n");
const reads = {
  note: "Three reads on api.mainnet-beta.solana.com, nothing signed or sent. (1) RENT: getMinimumBalanceForRentExemption for 0, 165 and 170 bytes, and the rent sysvar itself (in jupiter-usdc-lmeow-pumpswap.json, the same minutes: 5,080 lamports a byte-year, threshold 1, so (128 + bytes) x 5,080). (2) AN ADDRESS SENT SOL BEFORE ITS ACCOUNT IS OPENED: two simulations (sigVerify false, replaceRecentBlockhash true) for `wallet` — the idempotent create of its LMEOW account alone, then an OUTSIDER's transfer of 650,240 lamports (the 0-byte minimum above) to that account's address followed by the same create, the wallet paying the fee. Alone, the wallet paid 1,518,840 (the 5,000 fee and 1,513,840 of rent); pre-funded, it paid 873,600 (10,000 of fees for the two signatures and 863,600 of rent), the outsider 650,240, and the account ended at 1,513,840 either way: the associated-token program tops a pre-funded address up to its minimum and charges the payer the difference only (its logs: a transfer, an allocate and an assign in place of one create). (3) SLOT PAIRS: twenty times, a getMultipleAccounts of `wallet` (processed) and right after it a simulateTransaction (processed) of a compute-budget-only transaction it pays for, 700 ms between pairs. Seven pairs answered from different slots, one apart each time; every pair that answered from one slot showed the wallet 5,000 lamports lower after (the simulated fee) and nothing else, while two of the seven split pairs showed 53,000 and 36,000: the wallet's own transactions landing between the read and the simulation.",
  capturedAt: p.capturedAt, rpc: p.rpc, wallet: p.wallet, outsider: p.outsider, mint: p.mint, tokenProgram: p.tokenProgram, ata: p.ata,
  minimumBalance: p.minimumBalance, preRead: p.pre, createAlone: p.createAlone, prefundedThenCreate: p.prefundedThenCreate, slotPairs: p.slotPairs,
};
fs.writeFileSync("/home/user/Cat-Intelligence-Agency/fixtures/agent/mainnet-rent-and-slot-reads.json", JSON.stringify(reads, null, 1) + "\n");
console.log("ok", fs.statSync("/home/user/Cat-Intelligence-Agency/fixtures/agent/jupiter-usdc-lmeow-pumpswap.json").size, fs.statSync("/home/user/Cat-Intelligence-Agency/fixtures/agent/mainnet-rent-and-slot-reads.json").size);
